# Word Counter Program
This program asks the user to type a full sentence and then counts how many words it contains.

## Features
- Asks the user to enter a sentence
- Splits the sentence into words using `split()`
- Counts the number of words with `len()`
- Prints the total word count

# How to use
Run the program and type any sentence when prompted.  
The program will display how many words the sentence contains.




# Learning Purpose
This project was created as part of my backend engineering practice.  
The goal was to improve my skills in Python, user input handling, string splitting, and basic list operations.
